/**
 * !注意：混入后添加init方法，不能声明onLoad
 */
export default {
    mixins: [],
    props: {},
    components: {},
    computed: {},
    data() {
        return {};
    },
    async onLoad(options) {
        // try {
        //     await this.$store.dispatch('loadUser');
        // } catch (e) {}
        // uni.hideLoading();
        // if (this.init) {
        //     this.init(options);
        // }
    },
    methods: {}
};
